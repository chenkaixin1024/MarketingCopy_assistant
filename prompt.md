
开发一个网页应用，核心功能为：把用户输入的文案，调用大模型 API 转化为花式排版。

功能和交互如下：
1. 首页包含一个文本输入框，允许用户输入原始文案，下方为“排版”按钮
2. 用户点击按钮后，把用户输入作为user_prompt连同预设的system_prompt，发送到大模型 API，user_prompt 使用如下方式拼接`待排版文案：\n<content>$user_input</content>`
3. 把模型返回的内容显示在输入框下方，支持重新生成
4. 主页提供一个配置入口按钮，进入配置页，可配置如下信息：
	4.1 模型 API 相关信息，包括 Base_url、Model_Name 和 API_KEY，存储在本地浏览器
	4.2 system_prompt系统提示词（用户未配置时默认使用“将如下文案进行花式排版”）

UI 细节
1. 使用 Shadcn/ui 风格的极简样式，但不要引入这些模块，使用纯 CSS 实现
2. 黑（#1E1E1E）白（#FFFFFF）灰（#EFEFEF）三色，不要其他颜色
3. 显示大模型返回内容时使用流式输出